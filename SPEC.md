# 影片浮水印工具 — 設計規格書

> 版本：v0.1（設計階段）
> 日期：2026-01-31
> 來源：徐士桓 提案 → Takuma 開發
> 狀態：無時程壓力，品質優先

---

## 1. 產品概述

跨平台桌面應用程式，用於在影片上疊加一個或多個浮水印圖片，
支援浮水印位置拖放、大小調整、透明度設定、動態移動效果，
以及批次處理多個影片檔案。

## 2. 目標平台
- macOS
- Windows
- Linux

## 3. 核心功能

### 3.1 影片匯入
- 點擊匯入影片檔案（支援 mp4, mov, avi, mkv 等常見格式）
- 自動判讀格式（直式 9:16 / 橫式 16:9 / 正方形 1:1 等）
- 顯示影片預覽圖（首幀或指定幀）
- 預覽區域根據影片比例自適應

### 3.2 浮水印管理
- 匯入浮水印圖檔（png, jpg, svg, gif）
- 匯入後自動疊加顯示在預覽圖上
- 預設最多顯示 3 個浮水印欄位
- 「更多...」按鈕可新增額外浮水印（無上限）
- 每個浮水印獨立設定以下屬性：

#### 位置
- 滑鼠拖放設定初始位置
- 顯示 X / Y 座標（像素或比例）

#### 尺寸
- 滑鼠拖放預覽圖上的浮水印邊角調整
- 或透過輸入框設定（比例 % 或像素 px）
- 鎖定比例 / 自由調整 切換

#### 透明度
- 點擊開啟對話框
- 滑桿 0%（全透明）~ 100%（不透明）
- 即時預覽效果

#### 移動方式（三選一，選定後鎖定）
| 模式 | 說明 | 額外設定 |
|------|------|---------|
| **固定** | 浮水印固定在指定位置不動 | 無 |
| **線性移動** | 沿路徑平滑移動，自動判讀影片邊界 | 移動速度（px/s 或 慢/中/快） |
| **隨機出現** | 在影片範圍內隨機位置閃現 | 閃爍頻率（秒/次） |

#### 「同上」功能
- 勾選後自動套用前一個浮水印的 **透明度** 和 **尺寸**
- **移動方式不受影響**，需各自獨立調整

### 3.3 選單功能

#### 儲存設定
- 將當前所有浮水印配置（尺寸、位置、透明度、移動方式）儲存為設定檔
- 下次開啟時可載入設定檔直接套用
- 設定檔格式：JSON 或 YAML

#### 批次處理
- 選取資料夾中的多個影片檔
- 套用當前浮水印設定到所有選取的影片
- 命名方式對話框：
  - **自行命名**：自訂前綴/後綴
  - **自動命名**：原檔名 + `_watermarked`
- 顯示批次處理進度條
- 處理完成通知

### 3.4 輸出
- 輸出格式：與原檔相同（或可選 mp4/mov）
- 輸出品質：可選（原始/高/中/低）
- 輸出路徑：原資料夾或自訂

## 4. UI/UX 設計原則

> ⚠️ 原始概念圖僅供功能參考，UI 需重新設計

### 設計方向
- **簡潔現代**：參考 Final Cut Pro / DaVinci Resolve 的簡潔風格
- **深色主題**：影片工具標配，減少眼睛疲勞
- **功能導向**：常用功能一步到位，進階設定收在面板中
- **即時預覽**：所有調整即時反映在預覽區

### 建議佈局
```
┌─────────────────────────────────────────────────┐
│  [≡ 選單]          影片浮水印工具          [─ □ ✕] │
├──────────────────────┬──────────────────────────┤
│                      │                          │
│                      │    浮水印屬性面板         │
│    影片預覽區域       │    ┌──────────────────┐  │
│    （可拖放浮水印）    │    │ 🖼 浮水印 1      │  │
│                      │    │ 位置 / 尺寸 / ... │  │
│                      │    ├──────────────────┤  │
│                      │    │ 🖼 浮水印 2      │  │
│                      │    │ 位置 / 尺寸 / ... │  │
│                      │    ├──────────────────┤  │
│                      │    │ ＋ 新增浮水印     │  │
│                      │    └──────────────────┘  │
├──────────────────────┴──────────────────────────┤
│  [📂 匯入影片]  [💾 儲存設定]  [⚡ 批次處理]  [▶ 輸出] │
└─────────────────────────────────────────────────┘
```

## 5. 技術待研究（由團隊確認）

| 項目 | 選項 | 需評估 |
|------|------|--------|
| **框架** | Electron / Tauri / Flutter Desktop / Qt | 跨平台能力、效能、打包大小 |
| **影片處理** | FFmpeg / GStreamer / 其他 | 浮水印合成效能、移動效果支援 |
| **預覽引擎** | Canvas / OpenGL / 原生影片播放器 | 即時預覽拖放效果 |
| **打包方式** | 各平台 installer | macOS .dmg / Win .exe / Linux AppImage |

## 6. 開發階段

| 階段 | 內容 |
|------|------|
| **Phase 0** | 技術研究 + UI/UX 設計 ← 目前 |
| **Phase 1** | 基礎框架 + 影片匯入 + 預覽 |
| **Phase 2** | 浮水印匯入 + 拖放定位 + 尺寸調整 |
| **Phase 3** | 透明度 + 移動效果（固定/線性/隨機）|
| **Phase 4** | 儲存設定 + 批次處理 |
| **Phase 5** | 跨平台打包 + 測試 |

---

*Phase 0 由團隊研究最佳技術方案 + 設計 UI/UX，完成後再進入開發。*
